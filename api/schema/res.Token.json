{
    "type": "object",
    "required": [
        "id",
        "name",
        "created"
    ],
    "additionalProperties": false,
    "properties": {
        "id": {
            "$ref": "./util/id.json"
        },
        "name": {
            "type": "string",
            "description": "Human Readable name of the API Token"
        },
        "created": {
            "$ref": "./util/created.json"
        },
        "token": {
            "type": "string",
            "description": "Token returned when initially created"
        }
    }
}
