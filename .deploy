{
    "profile": "CHANGE-ME",
    "artifacts": {
        "docker": [
            "raster-uploader:{{gitsha}}",
            "raster-uploader:task-identify-{{gitsha}}",
            "raster-uploader:task-titiler-{{gitsha}}"
        ]
    }
}
